# shellckech config

# here string/document
disable=SC3011 # In POSIX sh, here-strings are undefined. (w3011)
disable=SC3045 # In POSIX sh, read -d is undefined. (w3045)

# arrays
disable=SC3030 # In POSIX sh, arrays are undefined. (w3030)
disable=SC3054 # In POSIX sh, array references are undefined. (w3054)

disable=SC3024 # In POSIX sh, += is undefined. (w3024)
